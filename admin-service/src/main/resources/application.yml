server:
  port: 8620

spring:
  application:
    name: admin-service
  main:
    allow-bean-definition-overriding: true

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/commodity?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root